REPLACE:coin_laws = {
	law_category = socioeconomic

	gold_and_silver_coins = {
		country_modifier = {
			goods_gold_used_for_minting = yes
			silver_used_for_minting = yes
			minting_inflation_threshold = 0.03
			minting_income_factor = 0.1
		}
		estate_preferences = { crown_estate }
		ai_will_do = {
			if = {
				limit = { root = { capital.market = { "goods_supply_in_market(goods:goods_gold)" > 0 "goods_supply_in_market(goods:silver)" > 0 } } }
				add = { desc = "has gold and silver" value = 200 }
			}
		}
		years = 2
	}

	gold_coins = {
		country_modifier = {
			goods_gold_used_for_minting = yes
			goods_gold_impacts_inflation = 0.1
			silver_impacts_inflation = -1.0
			minting_inflation_threshold = 0.01
			minting_income_factor = 0.1
		}
		estate_preferences = { crown_estate }
		ai_will_do = {
			if = {
				limit = { root = { capital.market = { "goods_supply_in_market(goods:goods_gold)" > 0 } } }
				add = { desc = "has gold" value = 100 }
			}
		}
		years = 2
	}

	silver_coins = {
		country_modifier = {
			silver_used_for_minting = yes
			silver_impacts_inflation = 0.1
			goods_gold_impacts_inflation = -1.0
			minting_inflation_threshold = 0.01
			minting_income_factor = 0.1
		}
		estate_preferences = { crown_estate }
		ai_will_do = {
			if = {
				limit = { root = { capital.market = { "goods_supply_in_market(goods:silver)" > 0 } } }
				add = { desc = "has silver" value = 100 }
			}
		}
		years = 2
	}

	copper_coins = {
		country_modifier = {
			copper_used_for_minting = yes
			copper_impacts_inflation = 0.1
			goods_gold_impacts_inflation = -1.0
			silver_impacts_inflation = -1.0
			minting_inflation_threshold = -0.025
			minting_income_factor = 0.2
		}
		estate_preferences = { crown_estate }
		years = 2
	}

	custom_union_of_the_rhine_policy = {
		country_modifier = {
			goods_gold_used_for_minting = yes
			goods_gold_impacts_inflation = 0.1
			silver_impacts_inflation = -1.0
			global_merchant_capacity_modifier = 0.1
			monthly_diplomats = 0.05
		}
		estate_preferences = { clergy_estate burghers_estate }
		years = 2
	}

	hanseatic_coins_policy = {
		country_modifier = {
			goods_gold_used_for_minting = yes
			silver_used_for_minting = yes
			minting_inflation_threshold = 0.01
			bank_interest = -0.01
		}
		estate_preferences = { burghers_estate }
		years = 2
	}

	bullion_coins = {
		country_modifier = {
			silver_used_for_minting = yes
			copper_used_for_minting = yes
			goods_gold_impacts_inflation = -1.0
			minting_inflation_threshold = 0.02
			minting_income_factor = 0.1
		}
		estate_preferences = { crown_estate }
		ai_will_do = {
			if = {
				limit = { root = { capital.market = { "goods_supply_in_market(goods:silver)" > 0 "goods_supply_in_market(goods:copper)" > 0 } } }
				add = { desc = "has silver and copper" value = 200 }
			}
		}
		years = 2
	}

	barter_exchange = {
		country_modifier = {
			goods_gold_impacts_inflation = -1.0
			silver_impacts_inflation = -1.0
			minting_inflation_threshold = 0.01
			minting_income_factor = 0.01
		}
		estate_preferences = { crown_estate }
		ai_will_do = {
			if = {
				limit = { capital.market = { "goods_supply_in_market(goods:goods_gold)" <= 0 "goods_supply_in_market(goods:silver)" <= 0 "goods_supply_in_market(goods:copper)" <= 0 } }
				add = { desc = "has no metals" value = 200 }
			}
		}
		years = 2
	}

	stavraton_coin_policy = {
		unique = yes
		potential = { has_unlocked_policy_trigger = { type = stavraton_coin_policy } }
		country_modifier = {
			silver_used_for_minting = yes
			copper_used_for_minting = yes
			minting_inflation_threshold = 0.03
			minting_income_factor = 0.1
			monthly_towards_capital_economy = societal_value_monthly_move
		}
		estate_preferences = { crown_estate burghers_estate }
		ai_will_do = { add = { value = 100 desc = "we always want this because it's better" } }
		years = 2
	}
}
